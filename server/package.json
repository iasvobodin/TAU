{
  "name": "bun-server",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.18.1",
    "prisma": "^6.6.0"
  },
  "scripts": {
    "build": "bun build --entrypoints ./index.ts --outdir ./dist --target node",
    "compile": "bun build --compile --minify --sourcemap ./index.ts --outfile server"
  },
  "peerDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/websocket": "^10.0.1",
    "@prisma/client": "^6.6.0",
    "fastify": "^4.29.0",
    "fastify-plugin": "^4.5.1"
  }
}